# cli.denosdk.deps

Dependencies installed when installing an app built with Deno:

```sh
$ slack create cli.denosdk.deps --template zimeg/slacks --branch cli.denosdk.deps
$ cd cli.denosdk.deps
$ slack run
```
